---
description: 禁止无故删除已有注释；仅当相关逻辑一并删除时可移除
alwaysApply: true
---

# 保留已有注释

- **不要删除**仓库里已有的注释（含 `#`、`//`、`/* */`、HTML/Vue 注释、YAML/工作流步骤说明等）
- **例外**：对应代码/配置逻辑已被删除或整体替换、注释已完全失效时，可一并删除过时注释
- 改写实现时：优先**保留并微调**原注释，使其仍准确；不要为「精简」或重写文件而清空注释
- 新增代码仍按项目约定补充简体中文注释；不得用「重写整文件」绕过本规则
